colorlog>=6.9.0
mypy>=1.13.0
pre-commit>=4.2.0
ruff>=0.4.0

# For tests
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.0.0
# The newer HA test stack requires aiodns 4.x, which in turn requires pycares 5.x.
pycares<6
